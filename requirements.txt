# GUI
PyQt6>=6.6,<7.0

# Audio recording & playback
sounddevice>=0.4.6
scipy>=1.11
numpy>=1.26

# Screenshots
Pillow>=10.0

# Global hotkey (Windows & Linux; on Linux this typically needs root
# or a udev rule to read raw input devices -- Night Focus keeps working
# without it, just without the global shortcut)
keyboard>=0.13.5

# Packaging (optional, only needed to build a standalone executable)
# pyinstaller>=6.0
